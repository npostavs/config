[core]
	attributesfile = "~/.config/git/attributes"
	# Drop CRs before committing, probably a good default most of the time.
	autocrlf = input
[color]
	ui = auto
[user]
	email = npostavs@users.sourceforge.net
	name = Noam Postavsky\n
	signingkey = npostavs
[alias]
	co = checkout
	ci = commit
	graph = log --oneline --graph --decorate
	hub = !~/.config/git/github-fetch.sh
	pr = !~/.config/git/pr.sh
	mkpatch = difftool --no-prompt --extcmd 'diff -u --label old/$BASE --label new/$BASE'
[push]
	default = simple
[rerere]
	enabled = true
[merge]
	conflictstyle = diff3
	branchdesc = true
[gc]
	# default is 60 days, but better not to throw out human work too
	# quickly
	rerereresolved = 365
[diff]
	mnemonicprefix = true
[diff "lisp"]
	# lisp diff headers should lines starting with parens
	xfuncname = "^\\(.*$"
[diff "c"]
	# static void func(args)
	# func (args) // return on previous line
	# DEFUN ("func", ...) // Emacs macro
	xfuncname = "^(([a-zA-Z0-9_]+ ?){1,2}[()].*)"
[diff "org"]
	# org headers start with stars
	xfuncname = "^ *[*]+.*$"
